# Bessel and Airy family

## Background

AMOS uses the Bessel and Airy families as the main numerical workhorses for complex special functions. The implementation switches among series, recurrence, asymptotic, and continuation paths depending on region.

## Math

```math
Ai(z) = \frac{1}{\pi\sqrt{3}}\sqrt{z}\,K_{1/3}\left(\frac{2}{3}z^{3/2}\right),
\qquad
Ai'(z) = -\frac{1}{\pi\sqrt{3}} z\,K_{2/3}\left(\frac{2}{3}z^{3/2}\right)
```

```math
I_\nu(z) = i^{-\nu} J_\nu(iz),
\qquad
Y_\nu(z) = \frac{J_\nu(z)\cos(\pi\nu) - J_{-\nu}(z)}{\sin(\pi\nu)}
```

```math
H_\nu^{(1)}(z) = J_\nu(z) + iY_\nu(z),
\qquad
H_\nu^{(2)}(z) = J_\nu(z) - iY_\nu(z)
```

## Modifications and implementation

The translated implementation keeps the original split between the small-$|z|$ power-series path and the large-$|z|$ Bessel path described in [amos/zairy.f](https://github.com/qchen-fdii-cardc/openspecfun/blob/main/amos/zairy.f). The sequence routines also preserve the scaling conventions from AMOS so that underflow-sensitive regions stay numerically stable.

## Examples

```julia
using OpenSpecFun

zairy(1.0, 0.0, 1, 1)
zbesi(1.0, 0.0, 0.0, 1, 1)
```

## Charts

The charts below are generated by `docs/scripts/plot_reference_charts.jl`.

### Airy `Ai(x)`

![Airy Ai chart](../assets/airy_ai.svg)

### Airy `Ai'(x)`

![Airy derivative chart](../assets/airy_aip.svg)

### Dawson integral

![Dawson chart](../assets/dawson.svg)